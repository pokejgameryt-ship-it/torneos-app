# Server
PORT=3001
NODE_ENV=production
JWT_SECRET=replace-with-output-of-openssl-rand-base64-32
APP_URL=https://api.torneos-app.web.app

# Client (Vite build-time variables, prefix with VITE_)
# Use this when deploying to a remote backend. Leave empty for local dev.
VITE_API_URL=https://api.torneos-app.web.app
